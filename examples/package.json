{
  "name": "@pixelbox/examples",
  "version": "0.1.0",
  "private": true,
  "description": "PixelBox 示例应用集合(统一类型检查与打包验证)",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build-all.mjs && node scripts/test-electronic-perler.mjs",
    "check": "node scripts/build-all.mjs && node scripts/test-electronic-perler.mjs",
    "test": "node scripts/test-electronic-perler.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.23.1",
    "typescript": "^5.5.4"
  }
}
