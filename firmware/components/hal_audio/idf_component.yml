# hal_audio 组件依赖(ESP 组件注册表)
dependencies:
  idf: ">=5.3"
  # ES8311 codec 驱动(I2S 数据 + I2C 控制统一抽象)
  espressif/esp_codec_dev: "^1.3.0"
  # MP3 解码(esp_audio_simple_dec)
  espressif/esp_audio_codec: "^2.0.0"
